name: mobile_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.2+12

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # --- Core & Architecture ---
  flutter_riverpod: 
  riverpod_annotation: 
  provider: 
  go_router: 
  flutter_dotenv: 
  shared_preferences: 
  intl: 
  path: 

  # --- Permissions ---
  permission_handler: 

  # --- Firebase Ecosystem ---
  # NO VERSIONS: We let 'flutter pub get' calculate the perfect matching set.
  firebase_core: 
  firebase_auth: 
  firebase_ui_auth: 
  
  cloud_firestore: 
  firebase_ui_firestore: 
  
  firebase_storage: 
  firebase_ui_storage: 
  
  firebase_database: 
  firebase_ui_database: 
  
  firebase_messaging: 
  firebase_app_check: 
  firebase_remote_config: 
  firebase_analytics: 
  firebase_crashlytics: 
  firebase_performance: 
  google_sign_in: 
  cloud_functions: 

  # --- UI & Design ---
  cupertino_icons: 
  google_fonts: 
  flutter_svg: 
  lottie: 
  glass_kit: 
  shimmer: 
  animated_text_kit: 
  gap: 
  flutter_native_splash: 

  # --- Media & Files ---
  cached_network_image: 
  flutter_cache_manager: 
  image_picker: 
  flutter_image_compress: 
  photo_view: 

  # --- Utilities ---
  http: 
  url_launcher: 
  path_provider: 
  package_info_plus: 
  device_info_plus: 
  flutter_local_notifications: 
  table_calendar: 

  # --- Annotations ---
  json_annotation: 
  freezed_annotation: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0 # Kept loose for safety
  
  # --- Code Gen ---
  build_runner: 
  freezed: 
  json_serializable: 
  riverpod_generator: 
  custom_lint: 
  riverpod_lint: 
  flutter_launcher_icons: 

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon.png"
  min_sdk_android: 21 
  adaptive_icon_background: "#0F3460"
  adaptive_icon_foreground: "assets/icon.png"
  name: "EWUmate"

flutter:
  uses-material-design: true
  assets:
    - assets/lottie/
    - assets/icon.png
