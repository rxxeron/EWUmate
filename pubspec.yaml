name: mobile_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # --- Core & Architecture ---
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  go_router: ^14.2.0
  flutter_dotenv: ^5.1.0
  shared_preferences: ^2.2.3
  intl: ^0.20.0                  # FIX: Updated to match Flutter SDK requirement
  
  # --- Firebase Ecosystem ---
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.4
  firebase_ui_auth: ^1.13.1
  cloud_firestore: ^5.1.0
  firebase_ui_firestore: ^1.6.2
  firebase_storage: ^13.0.0      
  firebase_ui_storage: ^3.0.0
  firebase_database: ^11.0.5
  firebase_ui_database: ^1.4.1
  firebase_messaging: ^15.0.2
  firebase_app_check: ^0.3.0+2
  firebase_remote_config: ^5.1.0
  firebase_analytics: ^11.3.0
  firebase_crashlytics: ^4.0.5
  firebase_performance: ^0.10.0+5
  google_sign_in: ^6.2.1

  # --- UI & Design ---
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.10
  lottie: ^3.1.2
  glass_kit: ^2.0.1
  shimmer: ^3.0.0
  animated_text_kit: ^4.2.2
  gap: ^3.0.1
  flutter_native_splash: ^2.4.0
  
  # --- Media & Files ---
  cached_network_image: ^3.3.1
  flutter_cache_manager: ^3.3.2
  image_picker: ^1.1.2
  flutter_image_compress: ^2.3.0
  photo_view: ^0.15.0
  
  # --- PDF Viewer ---
  syncfusion_flutter_pdfviewer: ^26.1.41 
  syncfusion_flutter_core: ^26.1.41

  # --- Utilities ---
  http: ^1.2.1
  url_launcher: ^6.3.0
  path_provider: ^2.1.3
  package_info_plus: ^8.0.0
  device_info_plus: ^10.1.0
  flutter_local_notifications: ^17.2.1
  table_calendar: ^3.1.2
  
  # --- Annotations ---
  json_annotation: ^4.9.0
  freezed_annotation: ^2.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.11
  
  # --- Code Generation ---
  freezed: ^2.5.2
  json_serializable: ^6.8.0
  riverpod_generator: ^2.4.0
  
  # --- Linting ---
  custom_lint: ^0.6.4
  riverpod_lint: ^2.3.10
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon.png"
  min_sdk_android: 21 
  adaptive_icon_background: "#0F3460"
  adaptive_icon_foreground: "assets/icon.png"
  name: "Mobile App"

flutter:
  uses-material-design: true
  assets:
    - assets/lottie/
    - assets/icon.png
